import { describe,expect,it } from 'vitest'; import { documentBlocks,documentToMarkdown,documentToText,ensureBlockIds } from './document';
describe('document conversion',()=>{const doc={type:'doc',content:[{type:'heading',attrs:{level:2},content:[{type:'text',text:'設計'}]},{type:'paragraph',content:[{type:'text',text:'日本語の本文'}]}]};it('creates plain text by block',()=>expect(documentToText(doc)).toBe('設計\n日本語の本文'));it('creates markdown',()=>expect(documentToMarkdown(doc)).toContain('## 設計'));it('adds persistent block IDs',()=>expect(ensureBlockIds(doc).content?.[1].attrs?.blockId).toBeTruthy());it('repairs duplicate block IDs created by splitting a paragraph',()=>{const repaired=ensureBlockIds({type:'doc',content:[{type:'paragraph',attrs:{blockId:'same'},content:[{type:'text',text:'前'}]},{type:'paragraph',attrs:{blockId:'same'},content:[{type:'text',text:'後'}]}]});expect(documentBlocks(repaired).map(block=>block.blockId)).toEqual(['same',expect.not.stringMatching(/^same$/)])})});
