import { describe,expect,it } from 'vitest'; import { resolveAnchor } from './anchor';
describe('resolveAnchor',()=>{it('uses exact position first',()=>expect(resolveAnchor({blockId:'b',from:2,to:5,quote:'abc'},[{blockId:'b',text:'--abc'}]).orphaned).toBe(false));it('recovers moved quote with context',()=>expect(resolveAnchor({blockId:'b',from:0,to:3,quote:'対象',prefix:'前',suffix:'後'},[{blockId:'b',text:'対象 x 前対象後'}])).toMatchObject({from:6,to:8,orphaned:false}));it('keeps broken anchors as orphaned',()=>expect(resolveAnchor({blockId:'missing',from:0,to:1,quote:'x'},[]).orphaned).toBe(true))});
