import { describe,expect,it } from 'vitest'; import { documentToMarkdown,documentToText,ensureBlockIds } from './document';
describe('document conversion',()=>{const doc={type:'doc',content:[{type:'heading',attrs:{level:2},content:[{type:'text',text:'設計'}]},{type:'paragraph',content:[{type:'text',text:'日本語の本文'}]}]};it('creates plain text by block',()=>expect(documentToText(doc)).toBe('設計\n日本語の本文'));it('creates markdown',()=>expect(documentToMarkdown(doc)).toContain('## 設計'));it('adds persistent block IDs',()=>expect(ensureBlockIds(doc).content?.[1].attrs?.blockId).toBeTruthy())});
